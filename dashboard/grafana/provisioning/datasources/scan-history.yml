apiVersion: 1

datasources:
  - name: Scan History (CSV)
    type: marcusolsson-csv-datasource
    access: proxy
    url: http://metrics-server
    jsonData:
      path: scan-history.csv
      storage: url
    isDefault: true