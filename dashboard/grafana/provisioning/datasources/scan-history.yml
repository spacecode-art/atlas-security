apiVersion: 1

datasources:
  - name: Scan History (CSV)
    type: marcusolsson-csv-datasource
    access: proxy
    url: http://metrics-server/scan-history.csv
    jsonData:
      storage: http
    isDefault: true